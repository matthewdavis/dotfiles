# =============================================================================
# TERMINAL & DISPLAY
# =============================================================================
set -g default-terminal "tmux-256color"        # use 256 colors
set -ga terminal-overrides ",*256col*:Tc"      # enable true color support
set -g history-limit 100000                     # scrollback buffer n lines

# =============================================================================
# INDEXING
# =============================================================================
set -g base-index 1                             # start window indexing at one
setw -g pane-base-index 1                       # make pane numbering consistent

# =============================================================================
# PREFIX KEY
# =============================================================================
# Set prefix to Ctrl-a (like GNU Screen)
unbind C-b
set -g prefix C-a
bind C-a send-prefix                            # double tap C-a to send to shell

# =============================================================================
# MOUSE SUPPORT
# =============================================================================
set -g mouse on                                 # enable mouse (scroll, select panes, resize)
                                                # - Click and drag to select text (auto-copies)
                                                # - Middle-click or Ctrl-a+P to paste
                                                # - Scroll wheel to view history
                                                # - Click pane to switch, drag border to resize

# =============================================================================
# VI MODE & COPY/PASTE
# =============================================================================
setw -g mode-keys vi                            # use vi keys in copy mode

# Modern copy mode bindings (tmux 2.4+)
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel
bind-key -T copy-mode-vi 'r' send -X rectangle-toggle

# Enter copy mode with Escape
bind Escape copy-mode

# Paste from tmux buffer
bind P paste-buffer

# Window navigation
bind p previous-window
bind n next-window

# =============================================================================
# PANE MANAGEMENT
# =============================================================================
# Split panes using \ and -
unbind %
bind \\ split-window -h -c "#{pane_current_path}"  # vertical split (creates side-by-side panes)
bind - split-window -v -c "#{pane_current_path}"   # horizontal split (creates top/bottom panes)

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Vim-style pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# =============================================================================
# WINDOW MANAGEMENT
# =============================================================================
# Renumber windows when one is closed
set -g renumber-windows on

# =============================================================================
# ACTIVITY & NOTIFICATIONS
# =============================================================================
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity on
set-option -g bell-action none

# =============================================================================
# STATUS BAR - RED HAT THEME
# =============================================================================
set-option -g status-justify left
set-option -g status-interval 5                 # update status bar every 5 seconds
set-option -g status-position bottom

# Status bar colors - Red Hat branded
set-option -g status-style 'bg=#EE0000 fg=#FFFFFF'  # Red Hat red background, white text

# Left side of status bar
set-option -g status-left-length 40
set-option -g status-left '#[fg=#000000,bg=#FFFFFF,bold] #{session_name} #[fg=#FFFFFF,bg=#EE0000] #[fg=#FFFFFF,bg=#EE0000]#{?client_prefix,🔴 ,}#[fg=#FFFFFF,bg=#EE0000]'

# Right side of status bar
set-option -g status-right-length 150
set-option -g status-right '#[fg=#FFFFFF,bg=#8B0000] ↓#{download_speed} ↑#{upload_speed} #[fg=#FFFFFF,bg=#8B0000] #{cpu_fg_color}#{cpu_bg_color} CPU:#{cpu_percentage} #[fg=#FFFFFF,bg=#8B0000] #{ram_fg_color}#{ram_bg_color} RAM:#{ram_percentage} #[fg=#FFFFFF,bg=#EE0000] #{weather} #[fg=#FFFFFF,bg=#EE0000] %a %d %b #[fg=#000000,bg=#FFFFFF,bold] %I:%M %p '

# Window status format
set-window-option -g window-status-format '#[fg=#FFFFFF,bg=#8B0000] #I:#W '
set-window-option -g window-status-current-format '#[fg=#EE0000,bg=#FFFFFF,bold] #I:#W '

# Window title
set -g set-titles on
set -g set-titles-string '#T'

# =============================================================================
# PANE STYLING - RED HAT THEME
# =============================================================================
# Red Hat Brand Colors:
# Red:   #EE0000 (RGB: 238,0,0)
# White: #FFFFFF
# Black: #000000

# Set inactive/active window styles
set -g window-style 'bg=#1a1a1a'                # subtle dark grey for inactive
set -g window-active-style 'bg=#000000'         # pure black for active

# Pane borders - Red Hat red theme
set -g pane-border-style 'fg=#3a3a3a'           # dark grey for inactive borders
set -g pane-active-border-style 'fg=#EE0000 bg=#000000' # Red Hat red for active

# =============================================================================
# KEY BINDINGS
# =============================================================================
# Reload config
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# Clear history
bind C-k clear-history \; display-message "History cleared!"

# Useful built-in keybindings (no need to add these, just reminders):
# Ctrl-a + z      : Zoom/unzoom current pane (fullscreen toggle)
# Ctrl-a + {      : Swap pane with previous
# Ctrl-a + }      : Swap pane with next
# Ctrl-a + q      : Show pane numbers (press number to jump to pane)
# Ctrl-a + [      : Enter copy mode (scroll/search with vi keys)
# Ctrl-a + space  : Cycle through pane layouts
# Ctrl-a + !      : Break pane into new window

# Enable xterm keys (control arrow functionality)
set-option -g xterm-keys on

# Broadcast typing to all panes (like Terminator)
bind-key b setw synchronize-panes \; display-message "Synchronize panes: #{?pane_synchronized,ON,OFF}"

# Quick window selection
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# =============================================================================
# PERFORMANCE
# =============================================================================
# Faster command sequences
set -s escape-time 0

# Aggressive resize (useful when using grouped sessions)
setw -g aggressive-resize on

# =============================================================================
# TMUX PLUGIN MANAGER (TPM)
# =============================================================================
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'          # better clipboard integration
set -g @plugin 'tmux-plugins/tmux-resurrect'     # save/restore sessions
set -g @plugin 'tmux-plugins/tmux-continuum'     # auto-save sessions
set -g @plugin 'sainnhe/tmux-fzf'                # fuzzy find sessions/windows (Ctrl-a + f)
set -g @plugin 'tmux-plugins/tmux-cpu'           # CPU, RAM, GPU stats
set -g @plugin 'tmux-plugins/tmux-net-speed'     # Network upload/download speeds (no netstat needed)
set -g @plugin 'xamut/tmux-weather'              # Weather information

# =============================================================================
# PLUGIN CONFIGURATION
# =============================================================================
# tmux-cpu - Red Hat themed colors
set -g @cpu_low_fg_color "#[fg=#FFFFFF]"
set -g @cpu_medium_fg_color "#[fg=#FFFFFF]"
set -g @cpu_high_fg_color "#[fg=#000000]"
set -g @cpu_low_bg_color "#[bg=#EE0000]"
set -g @cpu_medium_bg_color "#[bg=#8B0000]"
set -g @cpu_high_bg_color "#[bg=#FFFFFF]"

set -g @ram_low_fg_color "#[fg=#FFFFFF]"
set -g @ram_medium_fg_color "#[fg=#FFFFFF]"
set -g @ram_high_fg_color "#[fg=#000000]"
set -g @ram_low_bg_color "#[bg=#EE0000]"
set -g @ram_medium_bg_color "#[bg=#8B0000]"
set -g @ram_high_bg_color "#[bg=#FFFFFF]"

# tmux-weather
set-option -g @tmux-weather-interval 15          # Update every 15 minutes
set-option -g @tmux-weather-location ""          # Auto-detect location (or set your city/zip)
set-option -g @tmux-weather-format "%c+%t"       # Weather condition + temperature
set-option -g @tmux-weather-units "u"            # Use US units (Fahrenheit)

# tmux-resurrect
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-vim 'session'

# tmux-continuum
set -g @continuum-restore 'off'                  # manual restore (Ctrl-a + Ctrl-r to restore)
set -g @continuum-save-interval '15'             # save every 15 minutes (Ctrl-a + Ctrl-s to save)

# tmux-yank
set -g @yank_selection_mouse 'clipboard'         # copy mouse selections to clipboard

# =============================================================================
# Initialize TMUX plugin manager (keep this line at the very bottom)
# =============================================================================
run '~/.tmux/plugins/tpm/tpm'
